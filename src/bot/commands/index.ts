import * as ping from './ping';
import * as rss from './rss';
import * as purge from './purge';
import * as trackCourse from './track-course';

export const commands = {
	ping,
	rss,
	purge,
	trackCourse,
};
